# 🔬 Code Reading Chapter Template

## Learning Path
| คำถาม | คำตอบ |
|--------|-------|
| ผู้เรียนต้องรู้อะไรมาก่อน? | (prerequisites) |
| บทนี้สอนแนวคิดอะไร? | (code structure / implementation details) |
| หลังอ่านจบ ผู้เรียนควรทำอะไรได้? | (อ่านโค้ดแนวนี้เองได้) |
| ไฟล์ที่จะอ่าน? | (file path) |

---

## 1. เป้าหมายบทเรียน
"เรากำลังจะอ่านไฟล์ `<path>` เพื่อเข้าใจ `<what>`"

## 2. ภาพรวมของไฟล์ — มันอยู่ในระบบตรงไหน
```
(System diagram — ชี้ว่าไฟล์นี้อยู่ตรงไหน)
```

## 3. โครงสร้างไฟล์
```
(path/to/file.ts)
├── imports
├── constants/config
├── main function/class
└── helpers
```

## 4. เดินอ่านทีละส่วน

### 4.1 Imports — บรรทัดที่ 1-10
```language
(โค้ด imports)
```
**อธิบาย:** (ทำไมต้อง import เหล่านี้)

### 4.2 ส่วนที่ 2 — บรรทัดที่ 11-50
```language
(โค้ด)
```
**อธิบาย:** (ทีละบรรทัด)

### 4.3 ส่วนที่ 3 — ...
(ทำต่อไปจนจบไฟล์)

## 5. จุดที่ซับซ้อน — อธิบายเพิ่ม
```language
(โค้ดที่ซับซ้อน)
```
**Deep Dive:** (อธิบาย concept เบื้องหลัง)

## 6. สรุปสิ่งที่เรียนรู้จากไฟล์นี้
| สิ่งที่เรียน | รายละเอียด |
|-------------|-----------|
| ... | ... |

## 7. จุดที่มือใหม่มักเข้าใจผิด
| ❌ | ✅ |
|----|-----|
| ... | ... |

## 8. แบบฝึกหัด
1. ลองแก้ฟังก์ชัน `X` ให้ทำ `Y` แทน
2. เพิ่ม feature `Z` เข้าไป
3. เขียน test สำหรับกรณี `W`

## 9. ไฟล์ที่เกี่ยวข้อง — ถ้าอยากอ่านต่อ
- `path/to/related-file-1.ts`
- `path/to/related-file-2.ts`
